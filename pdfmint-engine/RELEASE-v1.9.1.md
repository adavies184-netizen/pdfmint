# PDFBreeze Engine v1.9.1

- Ensures authenticated billing preflight requests are accepted from both
  `pdfbreeze.net` and `www.pdfbreeze.net`.
- Keeps additional deployment-specific origins while always retaining the
  production PDFBreeze origins.
